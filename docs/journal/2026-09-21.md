# 2026-09-21 — Première livraison (v0.1.0)

## Fait
- Electron : fenêtre transparente traversante, silhouette CSS, bulle question (clic + Ctrl+Espace).
- Cartes : 82 affichables sur 85 ; les 3 À VÉRIFIER exclues (filtre fermé par défaut).
- Recherche locale sans IA : synonymes + racines + couverture. 19/19 sur le jeu de référence.
- Synchro nocturne des 182 articles (2 pages d'API) ; contenu/ relu depuis main toutes les heures.
- Questions sans réponse journalisées, expurgées (chiffres, e-mails).
- electron-updater + workflow Release sur tag v* ; démarrage avec la session.
- .gitattributes (LF partout, CRLF pour .bat/.cmd/.ps1). Corrigé « aigýë » dans a-ecarter.md.

## Décisions
- Corps des articles : on garde un sac de mots-clés, jamais le texte (index, pas archive).
- koffi (user32) ajouté : rendre le clavier à la bonne fenêtre ; servira au contexte par titre.
- Seuil : score ≥ 5 et, sans synonyme, ≥ 50 % des mots de la question retrouvés.

## Pièges rencontrés (ne pas réintroduire)
- `setFocusable(false)` sous Windows appelle blur() : le clavier part à la fenêtre suivante.
- Rendre la fenêtre cliquable déclenche un mouseleave parasite : vérifier le point quitté.
- `ready-to-show` ne se déclenche pas ici : afficher sur `did-finish-load`.
- L'avatar peut repasser sous des fenêtres normales : toggle alwaysOnTop toutes les 5 s.
- En dev, le cache distant masquait les fichiers locaux : lecture locale hors installation.

## Recette
- OK sur ce PC : clics traversants, clic avatar, Ctrl+Espace depuis le Bloc-notes, retour
  du clavier, réseau coupé (réponse depuis le cache, mention « hors ligne »).
- Reste : Ctrl+Espace depuis Chrome et une appli plein écran (à faire à la main),
  Échap au clavier réel, et mise à jour réelle v0.1.0 → v0.1.1 (Release à publier).
